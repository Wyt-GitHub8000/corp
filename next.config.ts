import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /* Nextjs默认在开发阶段开启的是严格模式，严格模式下会在内部模拟组件装载和更新，让开发者提前发现组件频繁更新时存在的问题，在配置中可以关闭 */
};

export default nextConfig;
